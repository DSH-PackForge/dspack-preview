#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H

/* miniz is compiled directly into the DLL (static), so no export decoration. */
#ifndef MINIZ_EXPORT
#define MINIZ_EXPORT
#endif

#endif /* MINIZ_EXPORT_H */